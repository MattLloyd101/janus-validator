/**
 * Enumerates the supported Domain kinds.
 *
 * This is intentionally small today; it will expand as more concrete domains are added.
 */
export enum DomainType {
  FINITE_DOMAIN = 'FINITE_DOMAIN',
}
